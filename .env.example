# Inworld API credential (Base64 string used after "Basic " in Authorization header).
# NEVER commit the real value — copy this file to .env and fill it in locally.
INWORLD_API_KEY=

# Port for static site + WebSocket proxy (default 8787)
PORT=8787
